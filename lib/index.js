const cache = require('./cache')
const fetchComments = require('./fetch-comments')
const fetchTooltip = require('./fetch-tooltip')
const search = require('./search')

module.exports = {
  cache,
  fetchComments,
  fetchTooltip,
  search
}
